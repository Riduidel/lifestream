<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.ndx.lifestream</groupId>
		<artifactId>lifestream</artifactId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>
	<artifactId>my-lifestream</artifactId>
	<packaging>pom</packaging>
	<description>My very own lifestream, using all provided modules to create a valid output</description>
	<properties>
		<!-- site.dir is configured in profiles -->
		<site.dir>${project.basedir}/src/site/lifestream/</site.dir>
		<goodreads.dir>${site.dir}/</goodreads.dir>
		<wordpress.dir>${site.dir}/</wordpress.dir>
		<wordpress.site>https://riduidel.wordpress.com/</wordpress.site>
		<output.mode>jekyll</output.mode>
	</properties>
	<profiles>
		<profile>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<!-- Download content and transform it -->
			<id>download-goodreads</id>
			<build>
				<plugins>
					<plugin>
						<artifactId>maven-clean-plugin</artifactId>
						<version>2.5</version>
						<configuration>
							<filesets>
								<fileset>
									<directory>${site.dir}/goodreads/</directory>
									<includes>
										<include>**/*</include>
									</includes>
									<followSymlinks>false</followSymlinks>
								</fileset>
							</filesets>
						</configuration>
					</plugin>
					<plugin>
						<groupId>${project.groupId}</groupId>
						<artifactId>goodreads</artifactId>
						<executions>
							<execution>
								<id>download goodreads</id>
								<goals>
									<goal>goodreads</goal>
								</goals>
								<configuration>
									<mode>${output.mode}</mode>
									<output>${goodreads.dir}</output>
									<username>${goodreads.mail}</username>
									<password>${goodreads.password}</password>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<!-- Download content and transform it -->
			<id>download-wordpress</id>
			<build>
				<plugins>
					<plugin>
						<artifactId>maven-clean-plugin</artifactId>
						<version>2.5</version>
						<configuration>
							<filesets>
								<fileset>
									<directory>${site.dir}/wordpress/</directory>
									<includes>
										<include>**/*</include>
									</includes>
									<followSymlinks>false</followSymlinks>
								</fileset>
							</filesets>
						</configuration>
					</plugin>
					<plugin>
						<groupId>${project.groupId}</groupId>
						<artifactId>wordpress</artifactId>
						<executions>
							<execution>
								<id>download wordpress</id>
								<goals>
									<goal>wordpress</goal>
								</goals>
								<configuration>
									<mode>${output.mode}</mode>
									<output>${wordpress.dir}</output>
									<username>${wordpress.login}</username>
									<password>${wordpress.password}</password>
									<site>${wordpress.site}</site>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<!-- Generate website using JBake -->
			<id>jbake</id>
			<properties>
				<site.dir>${project.basedir}/src/main/jbake/content/</site.dir>
				<output.mode>jbake</output.mode>
			</properties>
			<build>
				<plugins>
					<plugin>
						<groupId>br.com.ingenieux</groupId>
						<artifactId>jbake-maven-plugin</artifactId>
						<executions>
							<execution>
								<id>generate_jbake</id>
								<!-- moved into compile phase to make sure files are generated before -->
								<phase>compile</phase>
								<goals>
									<goal>generate</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-war-plugin</artifactId>
						<version>2.4</version>
						<configuration>
							<!-- web.xml is not mandatory since JavaEE 5 -->
							<failOnMissingWebXml>false</failOnMissingWebXml>
							<!-- <filteringDeploymentDescriptors>true</filteringDeploymentDescriptors>
								<warSourceDirectory>WebContent</warSourceDirectory> <webResources> <resource>
								<directory>src/main/webapp2</directory> <filtering>true</filtering> </resource>
								</webResources> -->
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>